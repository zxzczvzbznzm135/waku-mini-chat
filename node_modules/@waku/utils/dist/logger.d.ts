import { Debugger } from "debug";
export declare class Logger {
    private _info;
    private _warn;
    private _error;
    private static createDebugNamespace;
    constructor(prefix?: string);
    get info(): Debugger;
    get warn(): Debugger;
    get error(): Debugger;
    log(level: "info" | "warn" | "error", ...args: unknown[]): void;
}
