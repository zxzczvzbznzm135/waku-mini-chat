import { ContentMessage } from "./message.js";
/**
 * In-Memory implementation of a local store of messages.
 *
 * Messages are store in SDS chronological order:
 * - messages[0] is the oldest message
 * - messages[n] is the newest message
 *
 * Only stores content message: `message.lamportTimestamp` and `message.content` are present.
 */
export declare class MemLocalHistory {
    private items;
    get length(): number;
    push(...items: ContentMessage[]): number;
    some(predicate: (value: ContentMessage, index: number, array: ContentMessage[]) => unknown, thisArg?: any): boolean;
    slice(start?: number, end?: number): ContentMessage[];
    find(predicate: (value: ContentMessage, index: number, obj: ContentMessage[]) => unknown, thisArg?: any): ContentMessage | undefined;
    findIndex(predicate: (value: ContentMessage, index: number, obj: ContentMessage[]) => unknown, thisArg?: any): number;
    private validateMessage;
}
