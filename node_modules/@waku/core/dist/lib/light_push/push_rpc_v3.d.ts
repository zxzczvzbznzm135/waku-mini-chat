import { proto_lightpush as proto } from "@waku/proto";
import type { Uint8ArrayList } from "uint8arraylist";
/**
 * LightPush v3 protocol RPC handler.
 * Implements the v3 message format with correct field numbers:
 * - requestId: 1
 * - pubsubTopic: 20
 * - message: 21
 */
export declare class PushRpc {
    proto: proto.LightPushRequestV3 | proto.LightPushResponseV3;
    constructor(proto: proto.LightPushRequestV3 | proto.LightPushResponseV3);
    /**
     * Create a v3 request message with proper field numbering
     */
    static createRequest(message: proto.WakuMessage, pubsubTopic: string): PushRpc;
    /**
     * Create a v3 response message with status code handling
     */
    static createResponse(requestId: string, statusCode: number, statusDesc?: string, relayPeerCount?: number): PushRpc;
    /**
     * Decode v3 request message
     */
    static decodeRequest(bytes: Uint8ArrayList): PushRpc;
    /**
     * Decode v3 response message
     */
    static decodeResponse(bytes: Uint8ArrayList): PushRpc;
    /**
     * Encode message to bytes
     */
    encode(): Uint8Array;
    /**
     * Get request data (if this is a request message)
     */
    get request(): proto.LightPushRequestV3 | undefined;
    /**
     * Get response data (if this is a response message)
     */
    get response(): proto.LightPushResponseV3 | undefined;
    /**
     * Get the request ID
     */
    get requestId(): string;
    /**
     * Get the pubsub topic (only available in requests)
     */
    get pubsubTopic(): string | undefined;
    /**
     * Get the message (only available in requests)
     */
    get message(): proto.WakuMessage | undefined;
    /**
     * Get the status code (only available in responses)
     */
    get statusCode(): number | undefined;
    /**
     * Get the status description (only available in responses)
     */
    get statusDesc(): string | undefined;
    /**
     * Get the relay peer count (only available in responses)
     */
    get relayPeerCount(): number | undefined;
    /**
     * Check if this is a request message
     */
    private isRequest;
    /**
     * Check if this is a response message
     */
    private isResponse;
}
