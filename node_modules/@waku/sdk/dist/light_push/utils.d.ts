import { LightPushError } from "@waku/interfaces";
export declare const shouldPeerBeChanged: (failure: string | LightPushError) => boolean;
export declare const timeout: (timeout: number) => Promise<void>;
