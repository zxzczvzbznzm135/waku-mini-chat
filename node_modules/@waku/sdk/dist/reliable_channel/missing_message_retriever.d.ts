import type { IDecodedMessage, IDecoder, QueryRequestParams } from "@waku/interfaces";
import type { MessageId } from "@waku/sds";
export declare class MissingMessageRetriever<T extends IDecodedMessage> {
    private readonly decoder;
    private readonly retrieveFrequencyMs;
    private readonly _retrieve;
    private readonly onMessageRetrieved?;
    private retrieveInterval;
    private missingMessages;
    constructor(decoder: IDecoder<T>, retrieveFrequencyMs: number | undefined, _retrieve: <T extends IDecodedMessage>(decoders: IDecoder<T>[], options?: Partial<QueryRequestParams>) => AsyncGenerator<Promise<T | undefined>[]>, onMessageRetrieved?: ((message: T) => Promise<void>) | undefined);
    start(): void;
    stop(): void;
    addMissingMessage(messageId: MessageId, retrievalHint: Uint8Array): void;
    removeMissingMessage(messageId: MessageId): void;
    private retrieveMissingMessage;
}
