export declare class RetryManager {
    private retryIntervalMs;
    private maxRetryNumber;
    private timeouts;
    constructor(retryIntervalMs: number, maxRetryNumber: number);
    stopRetries(id: string): void;
    startRetries(id: string, retry: () => void | Promise<void>): void;
    private retry;
}
