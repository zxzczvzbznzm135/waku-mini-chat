import { HealthStatus, IWakuEventEmitter, Libp2p } from "@waku/interfaces";
type HealthIndicatorParams = {
    libp2p: Libp2p;
    events: IWakuEventEmitter;
};
interface IHealthIndicator {
    start(): void;
    stop(): void;
    toValue(): HealthStatus;
}
export declare class HealthIndicator implements IHealthIndicator {
    private isStarted;
    private readonly libp2p;
    private readonly events;
    private value;
    private readonly debouncedAssessHealth;
    constructor(params: HealthIndicatorParams);
    start(): void;
    stop(): void;
    toValue(): HealthStatus;
    private onPeerDisconnected;
    private onPeerIdentify;
    private assessHealth;
    private updateAndDispatchHealthEvent;
}
export {};
